[
  {
    "folder": "clean-escape",
    "expect": [],
    "note": "8 real cutouts from one vehicle (2022 Ford Escape). Nothing should be flagged."
  },
  {
    "folder": "injected-silverado-plus-tesla3",
    "expect": ["99-foreign.png"],
    "note": "8 real Silverado (truck) cutouts plus one injected Tesla Model 3 (sedan) cutout -- shape mismatch alone is enough to trip the 'certain' key (similarity 0.50 vs the 0.58 default floor)."
  },
  {
    "folder": "injected-escape-plus-camry",
    "expect": [],
    "note": "KNOWN-IMPERFECT: 8 real Escape cutouts plus one injected Camry cutout, NOT caught at the default threshold. Both vehicles are black, so the colour key doesn't discriminate (distance 17 vs the 110 ceiling), and a generic 3/4 SUV/sedan pose apparently sits close enough in CLIP embedding space that the shape key doesn't either (similarity 0.64, above the 0.58 certain floor and the doubtful floor needs colour agreement too). Pinned as a documented miss, not a target to fix -- see imaging/gallery.py's docstring for why a same-colour, similar-pose foreign cutout is the hard case this two-key check is weakest on."
  }
]
