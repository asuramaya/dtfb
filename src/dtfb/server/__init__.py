"""
Server mode: a CarCutter-API-shaped HTTP surface in front of dtfb's own
imaging pipeline. Opt-in (`pip install -e ".[server]"`) -- CLI mode never
imports this package. See app.py's module docstring for what/why/scope.
"""
